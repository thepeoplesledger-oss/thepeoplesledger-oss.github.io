# ============================================================
# THE PEOPLE'S LEDGER — Jekyll / GitHub Pages configuration
# Theme: Minimal Mistakes (loaded remotely, no local install needed)
# ============================================================
# HOW TO USE THIS FILE
# 1. Copy this entire file and paste it in as "_config.yml" at the
#    root of your GitHub repository (replace any existing file).
# 2. Filled in for you already: GitHub username (thepeoplesledger-oss)
#    and repository (thepeoplesledger-oss.github.io).
# 3. No public email was provided yet, so the "Email" contact links
#    have been left out for now. Once you have one, search for
#    "ADD EMAIL HERE" in this file for the two spots to add it back.
# 4. Commit the file. GitHub Pages will rebuild the site automatically.
# ============================================================
 
 
# ------------------------------------------------------------
# SITE IDENTITY
# ------------------------------------------------------------
locale                   : "en-US"
title                    : "The People's Ledger"
title_separator          : "-"
name                     : "The People's Ledger"
subtitle                 : "Truth, documented."
description              : "A civic accountability newsroom investigating the public, private, and informal actors shaping everyday life in Ghana."
masthead_title           : "The People's Ledger"
 
# Full web address of your site once it is live on GitHub Pages.
url                      : "https://thepeoplesledger-oss.github.io"
# Blank because thepeoplesledger-oss.github.io is a user/organization
# Pages site (repo name = username.github.io), which is served from
# the domain root rather than a project sub-folder.
baseurl                  : ""
repository               : "thepeoplesledger-oss/thepeoplesledger-oss.github.io"
 
# No image logo is used — the masthead shows clean text only,
# per the "no logo, text header" requirement.
logo                     :
og_image                 :
 
# Time zone used for post/collection dates.
timezone                 : "America/New_York"
 
 
# ------------------------------------------------------------
# APPEARANCE — high-contrast, serious editorial look
# ------------------------------------------------------------
# "contrast" is Minimal Mistakes' built-in high-contrast black/white
# skin — closest stock match to a Marshall-Project-style look.
minimal_mistakes_skin    : "contrast"
 
# Breadcrumbs give the site a structured, "newsroom section" feel
# similar to how The Marshall Project labels its story sections.
breadcrumbs              : true
 
# Site search (optional but standard on serious editorial sites).
search                   : true
search_full_content      : true
 
 
# ------------------------------------------------------------
# THEME
# ------------------------------------------------------------
# Remote theme means GitHub Pages builds Minimal Mistakes for you —
# you do not need to install or manage any theme files yourself.
remote_theme             : "mmistakes/minimal-mistakes"
 
 
# ------------------------------------------------------------
# PLUGINS
# ------------------------------------------------------------
plugins:
  - jekyll-remote-theme
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-paginate
  - jekyll-include-cache
  # ^ jekyll-include-cache is REQUIRED by Minimal Mistakes itself — the
  # theme's own template files use a Liquid tag called "include_cached"
  # internally. Without this plugin active, GitHub Pages fails to build
  # the ENTIRE site with "Unknown tag 'include_cached'". This was
  # missing before, which is why the site was stuck on an old build.
 
# jekyll-archives REMOVED: confirmed (by an actual failed build) that
# it is not on GitHub Pages' supported plugin list, and listing an
# unsupported plugin fails the entire site build, not just that one
# feature. Category/tag archive pages simply aren't achievable on
# GitHub's default Pages build. If you want them later, the fix is to
# switch this repo to a GitHub Actions build workflow (which can run
# any plugin in your Gemfile) — that's a bigger, separate change, not
# a _config.yml tweak. Ask any time and I can set that up.
 
# jekyll-paginate settings (used for the home page's story listing)
paginate                 : 10
paginate_path            : /page:num/
 
 
# ------------------------------------------------------------
# SEO + SOCIAL  (jekyll-seo-tag)
# ------------------------------------------------------------
# jekyll-seo-tag automatically builds canonical URLs from "url" +
# "baseurl" + each page's own address, so no extra setting is needed
# beyond what's already defined above.
twitter:
  username           :
og_image             :
 
# Organization identity used in SEO structured data.
social:
  type  : "Organization"
  name  : "The People's Ledger"
  links:
    - "https://github.com/thepeoplesledger-oss"
 
author:
  name            : "The People's Ledger"
  avatar          : false
  bio             : "Truth, documented."
  location        : ""
  links:
    - label: "GitHub"
      icon: "fab fa-fw fa-github"
      url: "https://github.com/thepeoplesledger-oss"
    # ADD EMAIL HERE once you have a public contact address:
    # - label: "Email"
    #   icon: "fas fa-fw fa-envelope-square"
    #   url: "mailto:you@example.com"
 
footer:
  links:
    - label: "GitHub"
      icon: "fab fa-fw fa-github"
      url: "https://github.com/thepeoplesledger-oss"
    # ADD EMAIL HERE once you have a public contact address:
    # - label: "Email"
    #   icon: "fas fa-fw fa-envelope-square"
    #   url: "mailto:you@example.com"
 
 
# ------------------------------------------------------------
# TOP NAVIGATION
# ------------------------------------------------------------
# Minimal Mistakes reads the top navigation menu from a SEPARATE
# file, not from _config.yml — that's a hard requirement of the
# theme itself. Your repo already has _data/navigation.yml with
# most of this wired up; see the separate navigation.yml delivered
# alongside this file for the corrected, complete menu (adds
# Weekly Record, Projects, and Contact, which were missing).
 
 
# ------------------------------------------------------------
# CONTENT MODEL — how your stories actually get organized
# ------------------------------------------------------------
# Your site does NOT use Jekyll "collections" — it uses regular
# blog posts in _posts/, each tagged with a "category:" front
# matter field matching a section slug (ledger-file, peoples-brief,
# weekly-record, investigations, editorials, projects). Each
# section then has its own plain landing page (e.g. investigations/
# index.md) that already exists in your repo. An earlier version of
# this file defined unused "collections:" — those did nothing (no
# matching folders existed) and have been removed here to match
# your real setup.
 
 
# ------------------------------------------------------------
# DEFAULTS — applied automatically so you never have to repeat
# this front matter in every single file
# ------------------------------------------------------------
defaults:
  # -------------------------- posts --------------------------
  # Applies to every story in _posts/ (Ledger File, People's Brief,
  # Weekly Record, Investigations, Editorials, and Projects pieces
  # all live here, distinguished by their "category:" front matter).
  - scope:
      path: ""
      type: posts
    values:
      layout: single
      author_profile: false
      read_time: true
      toc: true
      toc_label: "Contents"
      toc_icon: "list"
      comments: false
      share: false
      related: true
 
  # -------------------------- pages ---------------------------
  # Applies to your section landing pages (ledger-file/index.md,
  # investigations/index.md, about/index.md, contact/index.md, etc.)
  # Any front matter a page sets itself always wins over these.
  - scope:
      path: ""
      type: pages
    values:
      layout: single
      author_profile: false
      read_time: true
      toc: true
      toc_label: "Contents"
      toc_icon: "list"
 
 
# ------------------------------------------------------------
# READING / MARKDOWN SETTINGS
# ------------------------------------------------------------
markdown                 : kramdown
highlighter              : rouge
# Posts publish at /<category>/<title>/ — e.g. a post with
# "category: investigations" and title "Broken Locks" publishes at
# /investigations/broken-locks/, matching your section landing pages.
permalink                : /:categories/:title/
words_per_minute         : 200
 
kramdown:
  input                  : GFM
  hard_wrap              : false
  auto_ids               : true
  footnote_nr            : 1
  entity_output          : as_char
  toc_levels             : 1..6
  smart_quotes           : lsquo,rsquo,ldquo,rdquo
  use_coderay            : false
 
include:
  - _pages
exclude:
  - "*.gemspec"
  - "*.gem"
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
 
sass:
  sass_dir               : _sass
  style                  : compressed
